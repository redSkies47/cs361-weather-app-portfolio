input = 'weather-data\data.txt';

/**
 * Find the weather data with the the postal code
 * @param {String} _postalCode
 */
const findWeatherByPostalCode = async (_postalCode) => {
    // let's figure out this part
}